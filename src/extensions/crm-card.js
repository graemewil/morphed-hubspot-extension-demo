import { render } from "hubspot/ui-extensions";
import React from "react";

const MorphedCrmCard = () => {
  return (
    <div style={{ padding: "1em", fontFamily: "Arial" }}>
      <h2>Morphed AI Blueprint</h2>
      <p>This is a placeholder CRM card. You can customize this later.</p>
    </div>
  );
};

render(<MorphedCrmCard />);
